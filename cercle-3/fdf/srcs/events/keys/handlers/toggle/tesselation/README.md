# Tessellation Toggles

Handles tessellation, LOD, horizon culling, and target-point controls. This folder is responsible for switching tessellation on and off and adjusting the values that drive adaptive surface detail.

For exported declarations, see `includes/events.h`.