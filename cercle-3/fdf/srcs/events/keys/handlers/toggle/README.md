# Toggle Handlers

Handles one-shot toggles for rendering and camera behavior. This folder is responsible for filled-mode, adaptive logic, invert movement, tessellation toggles, horizon culling, and LOD-related settings.

For exported declarations, see `includes/events.h`.