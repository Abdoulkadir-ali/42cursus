# Map Handlers

Handles key actions that change the current map or map styling. This folder is responsible for resetting the camera style, cycling maps, generating new maps, toggling map color modes, and cycling GUI styles.

For exported declarations, see `includes/events.h`.