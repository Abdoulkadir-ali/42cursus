# View Handlers

Handles camera projection, movement, rotation, alignment, and movement-speed controls. This folder is responsible for both one-shot view changes and the per-frame helpers that consume held movement and rotation flags.

For exported declarations, see `includes/events.h`.