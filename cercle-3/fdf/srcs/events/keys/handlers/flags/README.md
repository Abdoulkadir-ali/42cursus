# Flag Handlers

Handles press/release state for keys that affect continuous behavior. This folder is responsible for tracking held directions, modifiers, and letter flags used by movement, rotation, and combo logic.

For exported declarations, see `includes/events.h`.