# Mouse Events Module

Handles mouse clicks, drags, and scroll input. This folder is responsible for tracking pressed buttons, interpreting drag gestures as camera or color adjustments, and using scroll input for zoom or zoom-speed changes.

For exported declarations, see `includes/events.h`.