# Event GUI Helpers

Handles window-event utilities tied to rendering and the GUI. This folder is responsible for rebuilding buffers after a resize and maintaining FPS counters for the GUI overlay.

For exported declarations, see `includes/events.h`.