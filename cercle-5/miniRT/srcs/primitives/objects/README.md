The goal of this module is to set up the objects.
It shouldn't execute

Meaning, if it's a mesh, setup the mesh, its triangle, apply the material, bvh

OBJECT is unaware of scene, it only sees includes/helpers